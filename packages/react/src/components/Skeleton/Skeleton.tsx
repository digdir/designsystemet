import React from 'react';
import cn from 'classnames';

import classes from './Skeleton.module.css';

/**  Skeleton component used for indicating loading elements */
export const Skeleton = (): JSX.Element => {
  return <></>;
};
