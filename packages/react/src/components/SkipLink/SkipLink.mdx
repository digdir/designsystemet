import { Meta, Canvas, Controls, Primary } from '@storybook/blocks';
import { Information } from '../../../../../docs-components';
import * as SkipLinkStories from './SkipLink.stories';

<Meta of={SkipLinkStories} />

# SkipLink

The skip link component is designed to assist users with disabilities, especially keyboard and screen reader users, in bypassing repetitive content and quickly accessing important sections or features of a web page. It enhances the accessibility and usability of a website.

<Primary />
<Controls />

## Bruk
