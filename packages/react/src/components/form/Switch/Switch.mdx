import { Meta, Canvas, Story, Controls, Primary } from '@storybook/blocks';
import { Information } from '../../../../../../docs-components';
import * as SwitchStories from './Switch.stories';

<Meta of={SwitchStories} />

# Switch

<Primary />
<Controls />

## Bruk

<Information text='token' />

```tsx
import '@digdir/design-system-tokens/brand/altinn/tokens.css'; // Importeres kun en gang i appen din.
import { Switch, Fieldset } from '@digdir/design-system-react';

<Fieldset legend='Profil instillinger'>
  <Switch>Mørk modus</Switch>
</Fieldset>;
```

## Gruppering

Bruker [Fieldset](/docs/felles-fieldset--docs) til gruppering.

<Canvas of={SwitchStories.fullWidth} />

### Høyrejustert

Bruk `position="left"` til å plassere label på venstre siden av `Switchen` dersom
du har behov for det.

<Canvas of={SwitchStories.fullWidthLeft} />
