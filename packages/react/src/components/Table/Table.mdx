import { Meta, Primary, Controls, Canvas, ArgsTable } from '@storybook/blocks';
import * as TableStories from './Table.stories';
import { TableHeaderCell } from './';

<Meta of={TableStories} />

# Table

<Primary />
<Controls />

# Sortering

`TableHeaderCell` har ein `sortable` prop som kan brukast for å sortere ein tabell. Denne må kombinerast med `onSortChange` propen som blir trigga når ein klikkar på ein `TableHeaderCell`.

<Canvas of={TableStories.Sortable} />

# Sticky header

<Canvas of={TableStories.StickyHeader} />

# With form elements

<Canvas of={TableStories.WithFormElements} />

## `TableHeaderCell` Props

<ArgsTable of={TableHeaderCell} />
