import { Meta, Canvas, Story } from '@storybook/addon-docs';
import { Tooltip, TooltipTrigger, TooltipContent } from './index';

import { Button } from './../Button/index';
import { Paragraph } from './../Typography/Paragraph/index';
import { Information, TokensTable } from '../../../../../docs-components';
import { ArgsTable } from '@storybook/addon-docs';

<Meta
  title='Felles/Tooltip'
  component={Tooltip}
/>

export const Template = (args) => {
  return (
    <Tooltip
      content='This is my content'
      {...args}
    >
      <Button>Hover or focus me</Button>
    </Tooltip>
  );
};

export const ComplexTemplate = (args) => {
  return (
    <Paragraph>
      Du kan ha{' '}
      <Tooltip content='Kan gi bra brukeropplevelse'>
        <span
          style={{ fontWeight: 'bold', textDecoration: 'underline dotted' }}
        >
          tooltip
        </span>
      </Tooltip>{' '}
      inne i tekst også
    </Paragraph>
  );
};

<Canvas>
  <Story
    name='Tooltip'
    decorators={[
      (Story) => (
        <div style={{ margin: '3em' }}>
          <Story />
        </div>
      ),
    ]}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story='Tooltip' />

### Plassering

<Canvas>
  <Story
    name='Tooltip with placement'
    decorators={[
      (Story) => (
        <div style={{ margin: '3em' }}>
          <Story />
        </div>
      ),
    ]}
    args={{
      placement: 'bottom',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story='Tooltip with placement' />

### Åpen som standard

<Canvas>
  <Story
    name='Tooltip with default open'
    decorators={[
      (Story) => (
        <div style={{ margin: '3em' }}>
          <Story />
        </div>
      ),
    ]}
    args={{
      defaultOpen: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story='Tooltip with default open' />

### Avansert bruk

<Canvas>
  <Story
    name='Tooltip in text'
    decorators={[
      (Story) => (
        <div style={{ margin: '3em' }}>
          <Story />
        </div>
      ),
    ]}
    args={{
      placement: 'bottom',
    }}
  >
    {ComplexTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story='Tooltip in text' />
