import { Meta, Canvas, Controls, Primary } from '@storybook/addon-docs/blocks';
import { CssVariables } from '@doc-components';
import css from '@digdir/designsystemet-css/tag.css?inline';
import * as TagStories from './tag.stories';

<Meta of={TagStories} />

# Tag

Tags er merkelapper som kan brukes til å kategorisere elementer eller kommunisere fremdrift, status eller prosess. Tags kan gi brukeren et raskere overblikk over innholdet. Bruk tags når innholdet har mange kategorier og det er hensiktsmessig å kunne skille mellom dem. Tags er statiske merkelapper; de kan ikke klikkes på.

<Primary />
<Controls />

## Bruk

```tsx
import { Tag } from '@digdir/designsystemet-react';

<Tag>Beta</Tag>;
```

## Kodeeksempler

## Sizes

<Canvas of={TagStories.Sizes} />

## Colors

<Canvas of={TagStories.Colors} />


## Retningsliner

- Bruk korte etiketter for enkel skanning. Helst ett ord. Maks to ord dersom det er nødvendig for å beskrive statusen og skille den fra andre tagger.
- Bruk stor forbokstav og små bokstaver for resten.
- Bruk ord og begreper som er gjenkjennelige for løsningen, unngå ukjente og fremmede ord.
- Tags bør som regel plasseres bak tittelen eller navnet på tilhørende element.
- Tags bør ikke plasseres sammen med knapper da det kan forvirre brukeren. Vurder plasseringen og brukertest gjerne.
- Vil du bruke flere tags sammen, bør du bruke en `<ul>` med en `<li>` for hver tag for å sikre at tagsene blir lest opp hver for seg som en liste og ikke som en setning.

**Passer til å** 
- Kategorisere innhold, for eksempel «Beta», «Ny» eller «Populær».
- Vise status, for eksempel «Pågående», «Fullført» eller «Avbrutt».

**Passer ikke til** 
- link til andre sider (bruk heller link).
- handlinger (bruk heller [button](/docs/komponenter-button--docs)).
- filtrering (bruk heller [Chip](/docs/komponenter-chip--docs)).

## Tekst


## CSS Variabler

<CssVariables css={css} />
