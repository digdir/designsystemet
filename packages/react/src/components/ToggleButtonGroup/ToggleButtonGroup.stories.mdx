import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { TokensTable, Information } from '../../../../../docs-components';
import { ToggleButtonGroup } from './ToggleButtonGroup';

<Meta
  title='Altinn/ToggleButtonGroup'
  component={ToggleButtonGroup}
/>

export const defaultArgs = {
  items: [
    {
      label: 'Kaffe',
      value: 'kaffe',
    },
    {
      label: 'Te',
      value: 'te',
    },
    {
      label: 'Vann',
      value: 'vann',
    },
  ],
  selectedValue: 'vann',
};

export const Template = (args = {}) => (
  <ToggleButtonGroup {...{ ...defaultArgs, ...args }} />
);

# ToggleButtonGroup

<Information text='altinn' />
## Variants
<Canvas>
  <Story name='Eksempel'>{Template.bind({})}</Story>
</Canvas>

## Egenskaper

<ArgsTable story='Eksempel' />

## Tokens

<TokensTable
  componentName='toggle_button'
  description
  jsonKey='component.toggle_button'
  showPreview={false}
/>
