{
  "name": "@digdir/designsystemet-css",
  "version": "0.6.0",
  "description": "CSS for Designsystemet",
  "author": "Designsystemet team",
  "repository": "https://github.com/digdir/designsystemet",
  "license": "MIT",
  "main": "./dist/index.css",
  "files": [
    "dist",
    "*.css"
  ],
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "css",
    "designsystem",
    "designsystemet",
    "digdir"
  ],
  "scripts": {
    "build": "rimraf dist && lightningcss --minify --bundle --browserslist ./*.css -d ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "lightningcss-cli": "^1.24.1",
    "rimraf": "^5.0.5"
  },
  "dependencies": {
    "@digdir/designsystemet-react": "workspace:^"
  }
}
