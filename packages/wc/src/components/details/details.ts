/** // TODO: Do we need a wrapper?
 * It might actually be nice to expose <u-details>, as this clarifies the Designsystem does no
 * additional functionality to the native <details> element. Same for <u-progress>
 * <u-tabs> though should probably be a <ds-tabs> element as it is not really a native HTML element yet.
 */
