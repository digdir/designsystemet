import { Meta } from '@storybook/addon-docs'
import {TokensTable, CodeSnippet} from "@digdir/storybook-components/src";

<Meta title="Design Tokens" />

# Design Tokens

As normal stash the on severely, contract, company he up were annoyed. Completely two based excuse the house. Even to and or the endeavours unrecognisable. Puzzles side he with anger origin their them. A watched o'clock he for are manipulate the.

## Kom i gang

[![](https://img.shields.io/npm/v/@digdir/ds-tokens?label=@digdir/ds-tokens&color=0051be)](https://www.npmjs.com/package/@digdir/ds-tokens)
[![](https://img.shields.io/badge/build-passing-brightgreen)](https://www.npmjs.com/package/@digdir/ds-core-react)
[![](https://img.shields.io/npm/dm/@digdir/ds-tokens.svg)](https://www.npmjs.com/package/@digdir/ds-tokens)
[![](https://img.shields.io/badge/licence-BSD3-yellowgreen?color=yellowgreen)](https://opensource.org/licenses/BSD-3-Clause)

Bruk av design tokens gjennom NPM (Node Package Manager).

### 1. Installer pakken i kommandolinjen ved hjelp av NPM, Yarn eller PNPM.

<CodeSnippet children='npm install @digdir/ds-tokens' language='markup'/>
<CodeSnippet children='pnpm install @digdir/ds-tokens' language='markup'/>
<CodeSnippet children='yarn add @digdir/ds-tokens' language='markup'/>


### 2. Importer og bruk variablene i ønsket format.

#### CSS

<CodeSnippet children='
@import "@digdir/ds-tokens/build/tokens.css";
:root {
  --my-text-color: var(--color-text-background-light);
}' language='css'/>

#### SCSS

<CodeSnippet children='
@use "@digdir/ds-tokens/build/tokens.scss" as tokens;
$my-text-color: tokens.$color-text-background-light;' language='scss'/>

#### Typescript

<CodeSnippet children='
import tokens from "@digdir/ds-tokens/build/tokens"
const myTextColor = tokens.color.text.background.light' language='javascript'/>

## Tokens

### Farger

<TokensTable jsonKey="colors" componentName="colors" />

### Avstander

Avstand-variablene er angitt i `rem`. Denne verdien er relativ til html-elementet sin `font-size`,
og tar utgangspunkt i den vanligste standarden der __1rem = 16px__.

`spacing-x` variablene speiler settet med definerte avstander som er tilgjengelig i designverktøyet (Figma).

`spacing-base-x` er et større sett med avstander til dersom man trenger flere verdier enn det som er definert i designet (for eksempel til å håndtere padding inni en komponent man bygger).
Variablene tar utgangspunkt i inkrement på 0.25rem (4px) fra `spacing-base-1` som starter på verdien 0.25rem, slik at `spacing-base-4` er det samme som `4 * 0.25rem == 4 * 4px == 4 * spacing-base-1`, nemlig 1rem (16px).


### Typografi

#### Tekststørrelser

Tekststørrelse-variablene er angitt i **rem**. Denne verdien er relativ til `html`-elementet sin `font-size`,
og tar utgangspunkt i den vanligste standarden der `1rem = 16px`.

<TokensTable jsonKey="font_size" componentName="font_size" />

#### Tekstvekting



#### Linjehøyder



#### Tekstfamilie

<TokensTable jsonKey="font_family" componentName="font_family" />

### Bruddpunkter

Bruddpunktene tar utgangspunkt i verdiene benyttet i [Bootstrap grid](https://getbootstrap.com/docs/5.1/layout/grid/), og følger de samme t-skjortestørrelsene. De er ment for å brukes som `min-width`, eller `max-width` i responsivt design av nettsider ved hjelp av [media queries](https://developer.mozilla.org/en-US/docs/Web/CSS/Media_Queries/Using_media_queries).

For eksempel vil `@media (min-width: var(--breakpoint-medium)) { ... } ` kunne brukes for å treffe de fleste tablets (og større skjermstørrelser).

<TokensTable jsonKey="breakpoints" componentName="breakpoints" />

### Kantlinjer

#### Kantlinjebredder


#### Kantlinjeradius


### Tid og varighet


### Z-indeks

