import { RulerIcon } from '@navikt/aksel-icons';

import { MenuPageLayout } from '@layouts';
import { Image, TokenList } from '@components';

export const metadata = {
  title: 'Størrelser og avstander',
  description: 'Størrelser og avstander',
};

export default ({ children }) => (
  <MenuPageLayout
    content={children}
    data={{
      title: 'Størrelser og avstander',
      date: '3. Januar 2025',
      icon: <RulerIcon />,
      color: 'blue',
    }}
  />
);


<TokenList
  type='dimension'
  hideValue={true}
/>
