import { Textfield } from '@digdir/designsystemet-react';
import { useState } from 'react';

export const WithCurrentColor = () => {
  return <Textfield label='Bytt ut dette eksempelet med SVG-eksempel' />;
};
