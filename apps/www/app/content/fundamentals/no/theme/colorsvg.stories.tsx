import { Textfield } from '@digdir/designsystemet-react';
import { useState } from 'react';

export const WithCurrentColor = () => {
  return <Textfield label='Bytt ut dette eksempelet med currentColor SVG-eksempel' />;
};

export const WithIconColor = () => {
  return <Textfield label='Bytt ut dette eksempelet med iconcolor token SVG-eksempel' />;
};

export const WithMultiColor = () => {
  return <Textfield label='Bytt ut dette eksempelet med multicolor tokens SVG-eksempel' />;
};

export const WithMultiColorInline = () => {
  return <Textfield label='Bytt ut dette eksempelet med multicolor tokens SVG-eksempel' />;
};