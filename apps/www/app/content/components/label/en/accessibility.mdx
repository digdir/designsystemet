A `<label>` must always be associated with a form element to ensure good accessibility.
This is done by using the `for` attribute (`htmlFor` in React), which points to the `id` attribute of the corresponding form element.
