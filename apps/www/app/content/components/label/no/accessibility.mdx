En `<label>` skal alltid være knyttet til et skjemaelement for å sikre god tilgjengelighet.
Dette gjøres ved å bruke `for`-attributtet (`htmlFor` i React) som peker til `id`-attributtet på det tilhørende skjemaelementet.
