<Story story="PreviewEn" />
<ReactComponentDocs />
## Use

```tsx
import { SkipLink } from '@digdir/designsystemet-react';

<SkipLink href="#main-content">Skip to main content</SkipLink>
```

## HTML

Add the class name `ds-skip-link` to an `<a>` tag that points to the main content of the page.

```html
<a class="ds-skip-link" href="#main-content">Skip to main content</a>

<!-- -->

<main id="main-content" tabindex="-1">
Region that can receive focus from skiplink.
</main>
```

## CSS variables and data attributes

<CssVariables />

<CssAttributes />
