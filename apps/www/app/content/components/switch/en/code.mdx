<Story story="Preview" />
<ReactComponentDocs />

## Usage

```tsx
import { Switch } from '@digdir/designsystemet-react';

<Switch label="Dark mode" value="dark" />
```

## Examples

### Grouping

Use [Fieldset](/en/components/docs/fieldset/code) to group multiple `Switch` components together.

<Story story="GroupEn" />

### Right-aligned
Use `position="end"` to position `Switch` on the right side of the prompt if
you need it.

<Story story="RightAlignedEn" />

## HTML
The class name `ds-input` is added to `<input>` along with `type="checkbox"` and `role="switch"`.

```html
<input
  class="ds-input"
  role="switch"
  type="checkbox"
/>
```

To mirror what the composite component in React does, you need to use `ds-field` together with a `<label>`.

To right-align `<input>`, add `data-position="end"` to `ds-field`.

```html
<div class="ds-field" data-position="end">
  <input
    class="ds-input"
    role="switch"
    type="checkbox"
    id="my-switch"
  >
  <label
    class="ds-label"
    data-weight="regular"
    for="my-switch"
  >Flightmode</label>
</div>
```

## CSS variables and data attributes
Switch is a composite component, so we show variables and data attributes from `ds-input` here.

<CssVariables />

<CssAttributes />
