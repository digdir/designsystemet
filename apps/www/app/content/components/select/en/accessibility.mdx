## Do not use a disabled state
Avoid using the disabled (`disabled`) state. Consider instead whether you need to show the field at all, or whether you can present the information as plain text or use `readOnly`.

## Keyboard navigation
- <kbd>Space</kbd> opens the list (also selects an option on Mac)
- <kbd>Enter</kbd> selects an option in the list (also opens the list on Windows)
- <kbd data-icon>↓</kbd> highlights the next option in the list
- <kbd data-icon>↑</kbd> highlights the previous option in the list
- <kbd>Home</kbd> highlights the first option in the list
- <kbd>End</kbd> highlights the last option in the list
- Other letters/characters jump to the first option that begins with that letter or character.
