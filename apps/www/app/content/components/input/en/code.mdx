<Story story="Preview" defaultOpen/>

## Usage

The class `ds-input` is used on most form controls, such as `<input>`, `<textarea>`, and `<select>`.

```html
<input class="ds-input"/>
<textarea class="ds-input"/>
<select class="ds-input"/>
```

For a valid form element, we recommend using `<ds-field>` together with a `<label>`.

```html
<ds-field class="ds-field">
  <label class="ds-label" >Checkbox</label>
  <input class="ds-input">
</ds-field>
```


## CSS variables and data attributes

Sizes are controlled with [data-size](/en/fundamentals/code/sizes) and colors with [data-color](/en/fundamentals/code/colors).
The component will inherit from the closest parent where these are set.

<CssVariables />

<CssAttributes />

## Validation and error state

By design, the Input does not automatically receive error styling based on the browser's built-in `:invalid` state.
To give you control over when the error state is displayed, we recommend instead showing and hiding an associated [Validation message](/en/components/docs/validation-message/overview).
When the [Validation message](/en/components/docs/validation-message/overview) and the Input are placed within the same [Field](/en/components/docs/field/overview), the Input automatically receives `aria-invalid="true"` when necessary.

## Examples

### ReadOnly

<Story story="ReadOnly" />


## React

```tsx
import { Input, Label } from '@digdir/designsystemet-react';

<>
  <Label htmlFor="my-input">Label</Label>
  <Input id="my-input" />
</>
```

<ReactComponentDocs />

### Type values
`Input` exposes only supported `type` values. You can see the valid type values in the dropdown below.
<Story story="InputType" layout="block" />
