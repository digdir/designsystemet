<Story story="Preview" defaultOpen/>

## Usage

You use either `<ul>` or `<ol>` with the class name `ds-list`.
`<li>` is used for each list item, and does not need a class name.


## CSS variables and data attributes
Sizes are controlled with [data-size](/en/fundamentals/code/sizes). The component will inherit the closest parent where this is set.

<CssVariables />

<CssAttributes />

## Example

### Unordered list

<Story story="UnorderedEn" layout="column" />

### Ordered list

<Story story="OrderedEn" layout="column" />

## React

```tsx
import { List } from '@digdir/designsystemet-react';

<List.Unordered>
  <List.Item>Item</List.Item>
</List.Unordered>
```
<ReactComponentDocs />