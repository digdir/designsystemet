`Link` works the same way as `<a>`, and its functionality has not been changed.
Focus is shown with a background around the link.

When a user has previously visited a link, the colour of the link will change.
