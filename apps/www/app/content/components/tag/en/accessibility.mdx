If you are displaying several `Tag` components together, we recommend using a `<ul>` list with one `<li>` per `Tag`. This ensures that they are announced as a list rather than as one continuous sentence, providing a better experience for users of assistive technologies.
