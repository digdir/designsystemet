import Meta from '../../components/Meta/Meta'
import PageLayout from "../../layouts/PageLayout/PageLayout";
export { getServerSideProps } from '../../utils/menus/PageMenu';

<Meta title='Universell utforming' />

export default ({ children, menu }) => <PageLayout Content={children} menu={menu}/>;

# Universell utforming
