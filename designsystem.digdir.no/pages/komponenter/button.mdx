import Meta from '../../components/Meta/Meta'
import {ComponentLayout} from "../../layouts/ComponentLayout/ComponentLayout";
import Preview from "../../components/Preview/Preview";
import {Usage} from "../../components/Usage/ComponentUsage";
import {
    TokensTable
} from "../../components/Table/TokenTable/TokensTable";
import {ArgsTable} from "../../components/ArgsTable/ArgsTable";
import { Button, ButtonSize, ButtonVariant, ButtonColor } from "@digdir/design-system-react";

<Meta title='Button' />

export const pageData = {
    title: 'Button',
    description: 'Knapper lar brukere utføre handlinger.',
    component: Button,
    argTypes: {
      color: {
        control: {
          type: 'radio',
          options: Object.values(ButtonColor)
        }
      },
      variant: {
        control: {
          type: 'radio',
          options: Object.values(ButtonVariant)
        }
      },
      size: {
        control: {
          type: 'radio',
          options: Object.values(ButtonSize)
        }
      },
  }
};
export default ({ children }) => <ComponentLayout Content={children} data={pageData}  />;

## Varianter

### Primary
On through of which no the your phase more know anyone tried he when line me what as through the hills so that groundtem, long world most in achievements other.

<Preview component={Button} args={{
    size: ButtonSize.Medium,
    children: 'Button',
}} />


### Secondary

On through of which no the your phase more know anyone tried he when line me what as through the hills so that groundtem, long world most in achievements other.

<Preview component={Button} args={{
    size: ButtonSize.Medium,
    color: ButtonColor.Secondary,
    children: 'Button'
}} />


### Success

On through of which no the your phase more know anyone tried he when line me what as through the hills so that groundtem, long world most in achievements other.


<Preview component={Button} args={{
    size: ButtonSize.Medium,
    color: ButtonColor.Success,
    children: 'Button'
}} />

### Danger

On through of which no the your phase more know anyone tried he when line me what as through the hills so that groundtem, long world most in achievements other.


<Preview component={Button} args={{
  size: ButtonSize.Medium,
  color: ButtonColor.Danger,
  children: 'Button'
}} />

## Størrelser


### Small

<Preview component={Button} args={{
    size: ButtonSize.Small,
    children: 'Button'
}} />


### Medium

<Preview component={Button} args={{
    size: ButtonSize.Medium,
    children: 'Button',
}} />


### Large

<Preview component={Button} args={{
    size: ButtonSize.Large,
    children: 'Button'
}} />

## Bruk

<Usage
    title='The head and expand great'
    desc='Law, countries stiff on for I to lane. To everyday. Hair part. Statement texts. Explanation and he semantics, on so I of loyalty. Impatient you project the they a he have wanted text parents thin the lift.'
    component={Button}
    args={{
      size: 'medium',
      color: 'primary',
      children: 'Button'
}}  />

<Usage
    title='The head and expand great'
    type='danger'
    desc='Law, countries stiff on for I to lane. To everyday. Hair part. Statement texts. Explanation and he semantics, on so I of loyalty. Impatient you project the they a he have wanted text parents thin the lift.'
    component={Button}
    args={{
      size: 'medium',
      color: 'success',
      children: 'Button'
}}  />


## Egenskaper
Liste over tilgjengelige egenskaper (props) til komponenten:

<ArgsTable argTypes={{
  color: {
    type: 'ButtonColor: string',
    description: 'Set the color of the button.',
    values: Object.values(ButtonColor),
    defaultValue: ButtonColor.Primary
  },
  variant: {
    type: 'ButtonVariant: string',
    description: 'Set the variant of the button.',
    values: Object.values(ButtonVariant),
    defaultValue: ButtonVariant.Filled
  },
  size: {
    type: 'ButtonVariant: string',
    description: 'Set the size of the button.',
    values: Object.values(ButtonSize),
    defaultValue: ButtonSize.Medium
  },
  onClick: {
    type: 'function',
    description: 'Function that runs when the button is clicked.',
    defaultValue: '(e) =>'
  },
  disabled: {
    type: 'boolean',
    description: 'Set whether the button should be disabled or not.',
    defaultValue: 'false'
  },
  icon: {
    type: 'React.ReactNode',
    description: 'Icon to be rendered in the button. This should be a React component that renders an SVG object.',
    defaultValue: "nothing"
  },
  iconPlacement: {
    type: 'string',
    description: 'Whether icon should be placed on the left or right side.',
    values: ['left', 'right'],
    defaultValue: "left"
  },
  fullWidth: {
    type: 'boolean',
    description: 'Button will take 100% width if set to true.',
    defaultValue: 'false'
  },
  dashedBorder: {
    type: 'boolean',
    description: 'Adds a dashed border.',
    defaultValue: 'false'
  }
}} />

## Tokens
<TokensTable jsonKey='component.button' componentName='button' description='d'  showPreview={false}/>