---
'@digdir/designsystemet': minor
---

feat(cli): add --verbose option to "tokens build" for easier debugging
fix(cli): update tokens template to reflect removal of `ingress` and renaming of `paragraph` to `body`
fix(cli): `tokens build` crashed when run on result of `tokens create`
