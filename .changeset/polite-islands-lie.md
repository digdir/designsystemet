---
"@digdir/designsystemet-css": minor
---

**New feature**: selection-tiles. Use attribute `data-selection-tile` or class `ds-selection-tile` to turn selection inputs (`radio`, `checkbox` and `switch`) into outlined tiles. Create more complex cases by combining the selector with `data-clickdelegatefor` on a larger surface.
