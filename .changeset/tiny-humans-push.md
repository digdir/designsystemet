---
"@digdir/designsystemet": patch
---

Change url for config schema on www.designsystement.no, from `cli` to `config` to better reflect which file the schema is for.
  ```json
  "$schema": "https://designsystemet.no/schemas/config/[VERSION].json"
  ```