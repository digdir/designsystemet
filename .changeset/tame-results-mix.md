---
"@digdir/designsystemet-css": minor
---

**toggle-group**: Changed border-radius to `--ds-border-radius-default`, border-color to `--ds-color-border-default` and color to `--ds-color-text-default`. Added new secondary design available with `data-variant="secondary"` (or `variant="secondary"` in react)
