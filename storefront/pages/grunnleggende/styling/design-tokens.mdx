import Meta from '../../../components/Meta/Meta';
import { PageLayout } from '../../../layouts';
export { getServerSideProps } from '../../../utils/menus/PageMenu';
import { Figma } from 'mdx-embed';

<Meta
  title='Design Tokens'
  description='Design tokens er gjenbrukbare verdier som fungerer som et bindeleddet mellom alle flater.'
/>

export default ({ children, menu }) => (
  <PageLayout
    content={children}
    menu={menu}
    data={{
      title: 'Design Tokens',
      date: '25. mars 2023',
    }}
  />
);

For å holde design og kode i sync bruker vi design tokens. Design tokens inneholder variabler for farger, typografi, størrelser, avstander og andre stilegenskaper. Vi bruker Figma-pluginen "Tokens Studio" for å opprette dem, men de blir lagret som en json fil, og er sånn sett ikke låst til en teknologi eller et designverktøy.

Tokens er fleksible verdier som sikrer at designere og utviklere jobber med de samme reglene og retningslinjene. Dette gjør det enklere å vedlikeholde designet og det legger også tilrette for å kunne ha et designsystem med ulike identiteter. 🎉 Vi kan lage en komponent i Figma kun èn gang og style den mange ganger - Ved et klikk kan vi slå på et annet theme som aktiverer f.eks en annen fargepalett:

## Tokens

Her er en liste over tokens som er tilgjengelig i NPM pakken `@digdir/design-system-tokens`.

<Figma
  title='Boop'
  url='file/vpM9dqqQPHqU6ogfKp5tlr/Felles-komponenter?node-id=6801%3A29775&t=igPXZdVyL2UpkpFe-1'
/>

### Farger

### Avstander

Avstand-variablene er angitt i `rem`. Denne verdien er relativ til html-elementet sin `font-size`,
og tar utgangspunkt i den vanligste standarden der **1rem = 16px**.

### Typografi

#### Tekststørrelser

Tekststørrelse-variablene er angitt i **rem**. Denne verdien er relativ til `html`-elementet sin `font-size`,
og tar utgangspunkt i den vanligste standarden der `1rem = 16px`.

### Bruddpunkter

Bruddpunktene tar utgangspunkt i verdiene benyttet i [Bootstrap grid](https://getbootstrap.com/docs/5.1/layout/grid/), og følger de samme t-skjortestørrelsene. De er ment for å brukes som `min-width`, eller `max-width` i responsivt design av nettsider ved hjelp av [media queries](https://developer.mozilla.org/en-US/docs/Web/CSS/Media_Queries/Using_media_queries).

For eksempel vil `@media (min-width: var(--breakpoint-medium)) { ... } ` kunne brukes for å treffe de fleste tablets (og større skjermstørrelser).

### Størrelser
