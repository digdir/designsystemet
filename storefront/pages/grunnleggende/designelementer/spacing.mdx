import Meta from '../../../components/Meta/Meta';
import { PageLayout } from '../../../layouts';
export { getServerSideProps } from '../../../utils/menus/PageMenu';

<Meta
  title='Avstander'
  description='Beskrivelse'
/>

export default ({ children, menu }) => (
  <PageLayout
    content={children}
    menu={menu}
    data={{
      title: 'Avstander',
      date: '5. april 2023',
    }}
  />
);

## Avstander

Avstand (spacing) brukes til å sette marging og padding. Settet med avstander som er definert i tokens sikrer konsistens på tvers av grensesnitt.
