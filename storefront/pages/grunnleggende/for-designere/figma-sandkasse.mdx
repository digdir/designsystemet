import Meta from '../../../components/Meta/Meta';
import { PageLayout } from '../../../layouts';
export { getServerSideProps } from '../../../utils/menus/PageMenu';

<Meta
  title='Figma sandkasse'
  description='Beskrivelse'
/>

export default ({ children, menu }) => (
  <PageLayout
    content={children}
    menu={menu}
    data={{
      title: 'Figma sandkasse',
      date: '5. april 2023',
    }}
  />
);

I sandkassen i Figma kan alle dele sine ideer og eksempler til det de ønsker skal være med i felles designsystem. Sandkassen gjør det enkelt å samhandle med designsystem-teamet og andre designere.
