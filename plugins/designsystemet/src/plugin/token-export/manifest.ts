import type { ModuleManifest } from '../../core/types';

export const tokenExportManifest: ModuleManifest = {
  id: 'token-export',
  title: 'Token-eksport',
  description:
    'Generer designsystemet-tokens fra en config og importer dem som Figma-variabler.',
};
